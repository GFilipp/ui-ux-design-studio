# Learnings (compounding memory)

Only human-confirmed wins and explicit kills go here, so the library stays clean (a mediocre
"win" reused as a template propagates mediocrity). One line per entry.

Format: `YYYY-MM-DD | brand | surface | WIN/KILL | what + why`

---
<!-- entries below -->
2026-06-23 | RocketMinds (example) | web | WIN | Product-as-hero from styled divs (a mock "today's shortlist" panel), zero images. Satisfies clean-type-first and passes the assets gate with no generated imagery; balances the hero so there is no dead zone.
2026-06-23 | RocketMinds (example) | web | WIN | Two-line headline: force the second clause onto its own line (accent span `display:block`) so the last line keeps >=2 words. The floor caught "call." orphaned on v1; the fix passed. The loop works: flag -> fix -> pass.
2026-06-23 | Aperture + RocketMinds (examples) | web | WIN | Brand-agnostic proven: the same hero section under two kits (RM dark/serif/orange vs Aperture light/sans/blue) renders as two different on-brand designs, both pass the floor, zero hardcoded brand.
2026-06-23 | engine | web | KILL | Browser-bridge window resize does NOT force a true mobile viewport (innerWidth stayed 1512 at a 390 request). Mobile-breakpoint floor checks need headless device emulation, not window resize.
2026-06-23 | engine | web | WIN | Mobile-first headless floor shipped: engine/floor/render.mjs (Playwright, bundled Chromium, explicit 390x844 + desktop 1440) runs extract.js via page.evaluate (kills the output-truncation limit) and writes screenshot + extract JSON per breakpoint. Replaces window-resize.
2026-06-23 | RocketMinds + Aperture (examples) | web | WIN | Mobile-first loop proven end-to-end: good.html FAILED the mobile gate (non-responsive 2-col grid → layout overflow + cramped panel), added a stack media query, re-ran → both kits pass mobile AND desktop. Flag → fix → pass.
2026-06-23 | engine | web | FIX | Orphan gate over-flagged body-paragraph rag on mobile (11 false orphans). Now flags only heading-like text (large/bold display OR <=6 words); long paragraphs exempt. The mobile gate caught a bug in the gate itself.
2026-06-23 | engine | web | WIN | Wired 4 FREE component MCPs at user scope (Claude Code), all health-check ✓: shadcn (`npx shadcn@latest mcp`), aceternity (`npx aceternityui-mcp`), flowbite (`npx flowbite-mcp`), 21st magic (`npx @21st-dev/magic@latest`, API key in ~/.claude.json — gitignored). Gotcha: `claude mcp add` variadic `-e` eats the next token; put the server NAME before `-e`. Built the origin-agnostic component-scout subagent (agents/component-scout.md) + the library-scan playbook (engine/scan/library-scan.md). Paid (Mobbin/Refero) remain opt-in.
2026-09-08 | engine | review-round-3 | FIX | Third audit of the day found FOUR HIGH items, two in the newest commit and one a fix I had CLAIMED in a commit message that never landed. (1) GATES LIVELOCK: growing GATES to 14 while `gate`/`override` validated names against the run FILE's keys (and unresolved_gates iterated the code's GATES) meant a pre-existing run file could never be resolved — `gate --name targets` said 'unknown gate', ship-check blocked, resting-ok failed, and the Stop hook blocked turn-end with remediation that could not work. Fixed by validating against GATES and backfilling missing keys in load(); the one affected file (garyfilipp-site, Jul 12) was cancelled with Gary's OK. (2) `--breakpoint` defaulted to the string 'desktop', which is always truthy, so the viewport fallback was unreachable: a 390px extract run the way the pod docs said to run it (no flag) SKIPPED type_size entirely and used the 24px pointer floor for Fitts — the two headline gates, silently off. Now default None (infer from viewport) + choices=[mobile,desktop]. (3) Non-flex nav links were EXEMPT from Fitts because `inlineInProse` was just display:inline + <a>; nine 18px `<li><a>` links produced zero findings. Now the parent must carry its own prose text. (4) The README quickstart I had rewritten that morning STILL could not reach SHIP OK: its gate loop omitted the four new gates and step 2 referenced files nothing creates. Verified this time by RUNNING it literally in a fresh repo through to `done`. PROCESS DEFECT behind two silent misses: my patch scripts printed 'done' after str.replace without checking the pattern matched — the exemplars fix and a dead-code removal both no-op'd invisibly. Every replace now asserts its match count or aborts. SECOND PROCESS DEFECT, caught only by mutation-checking my own new test: bash `for` loop variables are GLOBAL, so newrun's `for g in ... responsive` overwrote the caller's `$g` and my four GATES-membership cases all tested `responsive`; removing `console` from GATES turned zero tests red. Fixed with `local g` + a distinct name; M3 now reds exactly the console case. Also: desktop Fitts now WARNS (blocks only on touch) per Gary's call and five docs that already said so; type_size covers 2-word labels (9px prices/legal were passing); the nav choice rule fires on <nav><ul> (the `closest('nav') === g` clause was identical to the tagName test); a fixture comment claimed a 20px input while Chrome rendered it 26px (content-box padding) — fixed the FIXTURE to match its claim, box-sizing:border-box. Memory said kibo-ui was wired (dropped) and listed aceternity/21st-magic as current (replaced): corrected, since memory outranks general knowledge. UX-LAWS.md had misattributed characters-per-line to Miller's law (it is typographic measure) and its heading called two warn-only rows 'enforced'. Suite: 85 -> 114 cases, exit-code assertions for every BLOCK claim, newrun post-condition, resting-ok actually executed, positive assertions beside every absence assertion. STILL HONOR-SYSTEM (recorded, not fixed): the floor gates are SET by the model from floor_check output; only no_drawing is independently re-verified at ship, because ship-check has no render URL. Recording the URL in the run file and re-running the floor at ship is the next structural step.
2026-09-08 | engine | ux-laws + docs | WIN | Encoded the UX laws worth encoding and rejected the rest in writing. ENFORCED as gates: Fitts (`targets`: BLOCKS under 44px on touch, 24px warn on pointer, inline prose links exempt because they are text not hit areas), readability (`type_size`: BLOCKS under 12px body), Miller's measure half (`measure`: BLOCKS over ~130 chars/line). WARN-only: `density` (a single block over 120 words) and `choices` (Hick/Miller, a sibling set of 10+ or nav over 7) — warn because a 12-logo wall can be correct and a threshold would fake certainty. Together these make RULES 5 'no walls of text' partly MECHANICAL for the first time; it had zero enforcement. JUDGED by the human in brief §4b + DNA rubric, deliberately not thresholds: serial position, peak-end, Von Restorff, Gestalt four. Jakob is satisfied structurally by RULES 4. REJECTED in `engine/floor/UX-LAWS.md` WITH REASONS (so a screenshot of 'the 20 laws' cannot re-add them): Postel, Tesler, Parkinson, Zeigarnik, Doherty (needs a perf gate we do not have — asserting it without measuring is theatre), Pareto, Occam. The viral list is also ~18 principles, not 20: it prints Postel's law twice and counts a Fitts corollary as its own entry. BEST MOMENT: the new Fitts gate immediately failed the engine's OWN reference samples — 34px and 43px CTAs on a 390px viewport, one a single pixel under. I fixed the SAMPLES, not the threshold; a floor that exempts its own fixtures is decoration. Required extending `extract.js`, which had never captured interactive elements at all (an icon-only button or any <input> was invisible to every check): now `controls[]` + `groups[]` + per-node chars/lineCount/lineHeight. Also found `console`, `targets`, `type_size`, `measure` were BLOCKING in floor_check but ABSENT from GATES, so a build with console errors could still reach SHIP OK — 'console errors are gated' was documented in four places and enforced in none. GATES is now 14. Docs batch cleared alongside: the README quickstart documented a retired hand-extraction flow, a filename nothing produces, desktop-first against the mobile-first doctrine, and could never reach SHIP OK now that references are counted — rewritten as a runnable 7-step flow. Plus exemplars said 1 unresolved when 2 are, .gitignore missed render.mjs's actual artifact names, memory pointed at a non-existent engine/loop/floor_check.py, and `gate --status fail` exits 2 where everything else uses 3 (documented rather than changed, to avoid breaking callers). All 85 cases green and the new gates are mutation-checked.
2026-09-08 | engine | audit-round-2 | FIX | Re-audit of the morning's fix pass: FOUR of the eight bypasses I had reported CLOSED were re-openable, and I had introduced two new false positives. Root pattern: I GUARDED THE WRONG LAYER. (1) `vendored --add` validated the CLI writer, but `load_run_config` read `state["vendored"]` verbatim and the run file is model-owned JSON, so `"vendored": ["."]` still exempted the whole repo -> validate at CONSUMPTION. (2) One `git commit` emptied the scan: with no base_sha the degraded note was computed but never printed (stdout captured; hook only echoed on non-zero), and the documented 'integrate then git push' step is what hid the canvas -> degraded scans now exit 5 = cannot verify, which the existing fail-closed logic already blocks. (3) Padding PREPENDED still hid the drawing (I only tested appended) -> scan head AND tail. (4) Soft findings blocked nothing at ship-check/done while the hook blocked them, and the model can run ship-check->done in one turn before the hook fires -> both now block unless no_drawing is overridden. (5) human_pick was non-overridable but freely PASSABLE via `gate`; my test only covered the override half -> `gate --name human_pick` refused, new `pick --candidate <id>` records WHICH candidate was chosen. Honest limit now written into RULES 12: nothing in a CLI the model drives can PROVE a human chose; the mechanism makes usurping it deliberate and visible, not impossible. MY OWN NEW BUGS: the identifier regex took the last `X =` in a 400-char window, so `const rows = new Array(12)` before a getContext call made `rows.fill(0)` a NON-OVERRIDABLE block -> cut back to the current statement first; and `ownText` made a parent emit its child's text with the PARENT's color, so white-parent/dark-child reported contrast 1.0 on 18.9:1 text, unfixable by recoloring -> `ownsDirectText` guard. Also: extract.js mutated the live DOM per element while render.mjs screenshotted afterwards, so the human's pick image could differ from what was measured -> strip comments once, screenshot BEFORE extracting. THE BIGGEST LESSON IS ABOUT TESTS: four of my cases passed for the WRONG REASON — one stayed green with `drawing_check.py` DELETED (it asserted exit 3, which a missing scanner also returns), one asserted paths that did not exist under the run dir, two asserted exit codes that several unrelated gates produce. Exit codes are weak assertions; assert the MESSAGE or the result JSON. And every repaired case is now MUTATION-CHECKED: revert the fix, the test must go red. That check immediately found that my contrast fix had NO test at all (reverting it produced zero failures), which is precisely the blind spot the audit existed to expose. Suite: 75 cases. Also `os.path.normcase` is a no-op outside Windows, so case-aliased reference paths still double-counted on APFS -> dedupe references by st_dev/st_ino, which collapses hardlinks too.
2026-09-08 | engine | audit+tests | FIX | Fresh-eyes audit found the enforcement could be DEFEATED several ways; all 20 findings reproduced before fixing (failing-first) and are now named regression cases in a NEW runner, `engine/test/run-selftests.sh` (54 cases). ROOT CAUSE OF THE WHOLE BATCH: README claimed "self-tests in samples/" while NOTHING executed them — fixtures without a runner are decoration, and that is how a year of integrity bugs accumulated unseen. Closed: (1) `vendored --add .` exempted the ENTIRE repo in one command (no validation + prefix matching) -> entries must exist and live strictly inside the run dir; (2) EXCLUDE_SEGMENTS matched the ABSOLUTE path, so a repo living under any dir named build/out/dist silently voided every scan -> match relative to --root; (3) padding a file past 256KB downgraded the NON-OVERRIDABLE canvas block to exit 0 (severity 'warn' maps to 0) -> scan to the cap AND block on the unscanned remainder; (4) the references gate I had 'hardened' that morning was padded to 3 by ['https://','.','..'], directories, and one file spelled three ways -> isfile + a real host + realpath dedupe; (5) the canvas hard-block only ran at `done`, i.e. AFTER the documented git push -> ship-check runs it too; (6) drawing_hard returned False (=clean) on a missing scanner, a crash, or any unexpected exit -> fail closed on anything outside {0,2,3}; (7) `override --gate human_pick` let the model self-authorize the taste pick RULES 12 reserves for the human -> refused; (8) drawing_check's soft-finding exit 2 COLLIDED with argparse's usage-error 2, so a scanner that never ran read as scanned-and-waived -> usage errors exit 4. TWO BUGS I INTRODUCED WHILE FIXING, both caught by the new runner within minutes: a forward regex `[^;\n=]*getContext\(` backtracked catastrophically and hung the scanner for minutes on one 300KB line (fix: scan BACKWARD in a bounded window); and tightening canvas detection to 'primitive called on the bound identifier' created REAL FALSE NEGATIVES — `const c = el.getContext('2d'); paint(c)` with the drawing inside the helper, and a plain alias, both returned exit 0. Under-blocking is worse than over-blocking here, so canvas detection is now LAYERED: provable binding = hard (non-overridable), unprovable coexistence of a 2D context and draw primitives = soft (blocks, overridable). LESSON: when tightening a detector for precision, always test the false-NEGATIVE direction; precision gains silently trade away recall. Also fixed the orphan gate's blind spot: it skipped any heading with an element child, so `<h1>Build <span>faster</span></h1>` — the dominant marketing headline shape — was never measured and reported pass (directText gave totalWords=1, singleTextNode returned null). extract.js now measures across inline wrappers. Dropped kibo-ui the same day it was wired: its MCP endpoint returns HTTP 500 on initialize (verified twice, upstream, not the bridge); its shadcn registry still works. Codex's cross-model pass could NOT run — rate-limited both attempts — so every finding here came from local adversarial testing.
2026-09-08 | engine | wiring | FIX | 21st re-wired and VERIFIED END TO END. The dead stdio `magic` server (`npx @21st-dev/magic`, keys reset upstream) is replaced by the hosted `https://21st.dev/api/mcp` with an `x-api-key` header, server name `21st`. Proved the key works by raw MCP handshake rather than waiting a session: initialize returned serverInfo "21st 0.1.0" and tools/list returned 35 tools. GOTCHA in my own verification: the first tools/list looked EMPTY because the probe truncated the response body at 4000 chars mid-JSON, so the parse failed silently — a false negative from the test harness, not the server. Never truncate a body you then json.loads. BEST FINDING: the server exposes a `get_usage` tool that returns first-party plan truth, which beats any marketing page — it reports tier free, freeRetrievalsPerDay 2, searches unmetered, no AI allowance. That independently CONFIRMS the earlier correction that 21st free gives ZERO AI generations (the scan's "~5/month" was wrong). Operational rule now in the scout: browse with free `search`, spend the 2 DAILY `get_component` retrievals only on a component already chosen, check `get_usage` first. When a vendor MCP exposes a usage/quota tool, query it instead of trusting the pricing page.
2026-09-08 | engine | scan+gates | FIX | Acted on the quarterly library scan, but VERIFICATION KILLED TWO OF ITS HEADLINE CLAIMS — scans repeat vendor marketing, so check every price/free-tier claim against a primary page before deciding. (1) "21st free tier gives ~5 AI generations/month" is FALSE: free gives ZERO AI generations (help.21st.dev/ai/pricing); the "5" is a paid-plan trial of a different feature (Design Bug Bot reviews). Free = 2 component installs/day only. (2) "Lazyweb is free" is FALSE: the 281k screen library is $39/mo Individual / $99/mo Team; the free bearer token buys partial reports and locked recommendations. The gate is payment, not the token. SKIPPED it: ~4x Mobbin for the same category, plus a `curl|bash` installer carrying --discovery-path/--discovery-context telemetry and server-side experiment-gated tool availability. Actions taken: dropped the aceternity MCP (abandonware — v1.0.2, all 3 versions published within 18 minutes on 2025-07-19, one unofficial maintainer, 178 weekly downloads) and reached its OFFICIAL registry through shadcn instead (ui.aceternity.com/registry.json, 278 items, HTTP 200) — but this needs `"registries": {"@aceternity": ...}` in the TARGET repo's components.json or the shadcn MCP returns NOT_CONFIGURED, so it is per-repo config, not a global wire. Wired kibo-ui (MIT, no auth, remote via mcp-remote). KEY ALLOWLIST LESSON: kibo needed NO new VENDORED_SUBPATHS entry — its registry items are type registry:ui with files[].path "index.tsx", so they land in aliases.ui, already exempt. Verify `files[].path` in a library's registry JSON before adding an allowlist dir; a guessed dir is rot. Also closed the references gate's honor-system hole (same class as the drawing fix): `references[]` had NO writer and ">=3" was prose only, so the gate passed on the model's word. Now `run_state.py references --add` is the only way in (local paths must exist, so the count cannot be padded), `gate --name references --status pass` is REFUSED below 3, and ship-check/done reconcile the count against the array. Note the Stop hook still allows the human_pick resting state on a references shortfall — blocking there would recreate the 2026-07-07 deadlock — so that enforcement deliberately lands at `done`. Added a propagation checklist to library-scan.md: a scan that changes the library set must hand-update drawing_check VENDORED_SUBPATHS, the scout list, memory, and learnings, or the change half-lands. CODEX FOUND A SHIP BYPASS AFFECTING EVERY GATE (its review died on a ChatGPT usage limit mid-run, but not before this): ship-check and done tested `status == "halted"` to find blockers, so ANY unknown status ("approved", "ok", anything not literally "halted") satisfied both the blocker check AND the shortfall reconcile, and the run shipped. The hook's own resting-state check already used the correct `not in ("pass","overridden")` form; only run_state.py was wrong. Fixed with `unresolved_gates()`: a gate is resolved ONLY by exactly "pass" or "overridden", it iterates GATES rather than the file's keys (so a MISSING gate is unresolved instead of a KeyError crash), and it tolerates non-dict gate values. LESSON: fail-closed means enumerating what PASSES, never what blocks — an allowlist of good states, not a denylist of bad ones. Codex also independently reproduced two bugs self-review had already caught (references as a non-list crashed with TypeError; duplicate array entries double-counted), which is the value of the cross-model pass even when it dies early.
2026-07-03 | engine | wiring | FIX | Trailing carriage return (\r) in a credentials file silently breaks an MCP env key. `$(cat keyfile)` strips trailing \n but NOT \r, so the stored GEMINI_API_KEY was 54 chars (53-char key + \r) and would fail auth on the first render. Fix: wire with `tr -d '\r\n' < keyfile` AND clean the source file; always sanity-check a pasted key by comparing stored length vs stripped length. Separately: wired free component set expanded to six (shadcn / aceternity / flowbite / magic(21st) / magicui / heroui) + mcp-image (Nano Banana) for image gen, all user scope. De-hardcoded the library name-lists in RULES/design-pod/SKILL/component-scout so they enumerate enabled MCPs live (per no-hardcoded-rotting-values); the scout was already dynamic, the lists were stale illustration. Honest nuance: flowbite's MCP is figma-to-code + theme-gen, not component search, so it is not a browsable component source like the other five.
2026-06-24 | engine | audit | FIX | Post-rebuild fresh-eyes audit (Fable): closed 3 real defects: (1) "no console errors" was promised but unchecked; render.mjs now captures pageerror + console.error per breakpoint and floor_check blocks on them (older extracts without the key report "not-captured"); (2) brief-ok now honors an overridden brief gate (contract parity: pass OR loud logged override), previously override was meaningless; (3) em-dash rule corrected per Gary: RARE is fine, pileup is the AI tell; RULES reworded + warn-only style note at 3+ em dashes (never blocks). Plus: hook message names the three exits (brief-lock / override / cancel), SCHEMA documents optional kit fields (status, motifs, border/faint/accent2, type.mono), PLAN.md replaced with the rebuild plan, render.mjs falls back networkidle -> domcontentloaded on live-site timeout, pod ensures design-run.json is gitignored in TARGET repos.
2026-06-24 | engine | marketing-assets | FAIL then REBUILD | First live test (Tegy rebuild) failed entirely. Five root causes, each now structurally fixed: (1) the tool was a repo of scripts, never an installed skill, so I went rogue and hand-cranked HTML mockups (v1-v4); FIX: install as a real /design-studio skill, one path only. (2) no brief/value/story front-end, so endless correction; FIX: mandatory `brief` gate (run_state brief-ok) + engine/brief/brief-template.md, human approves once before any build. (3) hand-coded HTML cannot use the React component libraries, so never beautiful; FIX: RULES.md requires building with real components (Aceternity/21st/shadcn/Flowbite), never hand-coded. (4) fabricated product output presented as real; FIX: RULES.md forbids fabrication, real sources only. (5) constant babysitting; FIX: front-load to one brief approval + one pick. All hard rules in RULES.md, loaded by design-pod + skill. Tegy was the test case, not the subject; the tool is brand- and asset-agnostic.

## 2026-07-14 — Aggressive, provenance-based anti-hand-drawing
Goal: force components/mcp-image/type instead of Claude hand-drawing visuals.
KEY INSIGHT (red-team overturned the naive fix): shape alone cannot tell a
hand-drawn svg/canvas from a library one, because aceternity/magicui VENDOR raw
`<svg>`/`<canvas>` into source. A naive "block raw svg" scanner fires hardest on
the mandated libraries and rots into reflexive overrides. So provenance is the
primary filter: scan only Claude-AUTHORED composition files; exempt vendored dirs
(components.json aliases.ui + components/{ui,magicui,aceternity,motion-primitives,eldoraui,cult}
+ install-manifest) and brand assets.
Built: `engine/floor/drawing_check.py` (tiered — canvas getContext(2d)+draw
primitive = HARD block, non-overridable exit 3; illustration-scale svg
>=8 shapes / total d>=4000 / single d>=2500 = SOFT block, overridable exit 2;
icons <=4 shapes & d<=1200 or viewBox<=32 never flag). `extract.js` adds a
warn-only runtime graphics census (large svgs+canvases) that `floor_check.py`
surfaces to the human pick (runtime cannot do provenance, so never blocks).
`design-gate.sh` runs the scanner `--git-diff` INDEPENDENTLY as the reconciling
backstop: it blocks even when the model set `no_drawing=pass` (anti-honor-system),
inside the human_pick resting branch so the 2026-07-07 deadlock fix survives.
New `no_drawing` gate + `base_sha` + `vendored[]` in run_state/schema.
BUG caught in test: the hook's `set -e` killed the script on the scanner's
non-zero exit before branching; fixed with `|| DRC=$?`. Out of scope (stated
honestly in RULES): styled-div fake charts (no svg to detect) stay a human
Stage-3 brief-conformance call.
Codex cross-model review then closed 10 real bypasses before commit: (1) the
shadcn `components` alias exempted all authored `src/components/**` (now only the
specific `ui` alias, >=2 segments); (2) `vendored`/brand basename-matching exempted
every same-named file repo-wide (now resolved-abspath prefix match only); (3) a
model could override no_drawing then `done` to ship canvas (now `done` re-runs the
scanner and refuses hard findings non-overridably); (4) missing scanner failed
silently (now warns); (5) no-base + committed build missed everything (now
ls-files full-tree fallback); plus canvas Path2D/`.fill()`/`.stroke()` detection,
JSX `d={expr}` illustrations, `data:image/svg+xml;charset=,` decoded as base64
garbage, `git -z` filename parsing, `.mdx`, and oversized-file warn. ACCEPTED with
rationale (not fixed): deliberately moving art into a vendored dir evades by
construction (provenance targets the lazy default, not an adversary who edits
paths); soft-override authorizes the run (consistent with gate philosophy, reason
already required); runtime census is warn-only advisory.

## 2026-07-07 — Stop-hook deadlock at the human-pick gate
The design-gate Stop hook blocked EVERY turn-end while `human_pick` was the only
halted gate. But RULES 12 + design-pod forbid the model from passing or
overriding that gate — so a run legitimately parked at "candidates presented,
awaiting the human's pick" deadlocked the loop (hook demands progress the rules
forbid). Fix: design-gate.sh now allows the stop when brief has passed and the
ONLY unresolved gate is human_pick, with a stderr note. ship-check is untouched,
so Stage 4 still cannot ship without the pick. Lesson: state machines with
human-only gates need an explicit awaiting-human resting state, or their
enforcement hooks turn into infinite nags.

## 2026-07-07 — Decoration is not design (RM homepage redo)
First pass layered registry animations (border beams, dot patterns, hover
dimming, scroll timeline) onto unchanged layouts. Human verdict: gimmicky slop
that distracts a professional audience. Root cause: skipped Stage-0 grounding in
exemplars/INDEX.md and the DNA rubric ("zero decorative noise"), then let the
component-scout drive the design. The corpus already encoded the answer:
Tesla (type+space only), Exat (motion reveals one idea then pauses; my infinite
loops violated it), Tresmares (capital-firm premium-minimal = composition).
Redo that passed: stepped display hero, numbered index / ledger rows, unboxed
blockquotes, once-only reveals, fast hovers. Rule for the engine: exemplars and
rubric BEFORE the scout; the scout serves the composition, never replaces it.
2026-09-08 | engine | tooling | FIX | Preflight added (`engine/preflight.sh` + `TOOLS.md`) after a /design-studio run on garyflip-site reported four reference renders as failures: the pod wrapped `node render.mjs` in GNU `timeout`, macOS ships none, and nothing in the repo checked for any tool at all (the repo never called `timeout` itself; the hook uses a python wrapper). The registry is derived from what the engine invokes (python3, node, npm, npx, git, the Playwright module, a launchable browser) plus two recommended items (`timeout`, the Stop-hook wiring); `--install` installs via Homebrew/npm and re-checks; `--target <repo>` requires the package manager the target's lockfile implies (bun.lock -> bun, pnpm-lock -> pnpm, yarn.lock -> yarn; `packageManager` wins; npm suffices when its lockfile is among several). No version floor is typed anywhere: the node check reads `engines.node` from the installed Playwright. The pod runs it first in Stage 0 and halts on FAIL. Suite +19 cases (133) using a PATH farm (every executable on PATH symlinked into a temp dir minus the named tools) so "missing" is tested black-box without touching the machine; TOOLS.md parity with the registry is asserted. Lesson from the farm helper itself: bash joins `"$*"` with IFS[0], so building the exclusion string AFTER `local IFS=:` joined `bun pnpm` as `bun:pnpm` and hid nothing; the first prototype "passed" with bun still visible. Mutation M6 pins that.
